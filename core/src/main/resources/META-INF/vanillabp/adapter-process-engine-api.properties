# Filled by the Maven build (resource filtering). Read by the platform's VanillaBpParts to
# tell whether this adapter and the platform integration it runs on belong together.
part.version=${project.version}
part.artifact=${project.groupId}:${project.artifactId}
platform.version=${adapter-platform.version}
